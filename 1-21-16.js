function removeSmallest(numbers) {
	if(arr.length !==0) {
		var min = arr.reduce(function(a,b) {
			return Math.min(a, b);
		});
	}
  // throw "TODO: removeSmallest";
}

// ======================

removeSmallest([2,2,1,3,1])